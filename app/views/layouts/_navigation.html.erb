<nav class='navbar navbar-light .bg-danger'>
    <%= link_to 'RUBY NOTES', root_path, class: 'navbar-brand', id:'logo' %>
    <div class="navbar" id="navbarSupportedContent">
        <ul class="nav .nav-pills mr-auto">
        <% if logged_in? %>
            <li class="nav-item">
                <div>
                    <strong> <%= link_to 'BOOKS', books_path, class: "nav-link" %> </strong>
                </div>
            </li>
            <li class="nav-item">
                <div>
                    <strong> <%= link_to 'NOTES', notes_path, class: "nav-link" %> </strong>
                </div>
            </li>
        <% end %>
        </ul>
    </div>
    <div>
        <% if logged_in? %>
            <strong> <%= link_to "#{current_user.username}", user_path(current_user), class:"nav-link" %> </strong>
            <%= link_to 'Logout', logout_path, method: 'delete', class: "nav-link .bg-light .border-light" %>
        <% else %>
                <%= link_to 'Login', login_path, class: "nav-link"%>
                <%= link_to 'Sign Up', signup_path, class: "nav-link"%>
        <% end %>
    </div>
</nav>